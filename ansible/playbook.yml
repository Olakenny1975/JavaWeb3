---
- name: Update web servers
  hosts: webservers
 
  tasks:
- name: Get Artifact
  amazon.aws.s3_object:
    bucket: "{{ BUCKET_NAME }}"
    object: "{{ ARTIFACT_NAME }}"
    dest: "~/{{ ARTIFACT }}"
    mode: get
  delegate_to: localhost

